# V1 §7 — ConfigProperty reverse lookup

Status: FAIL

Expected: `spring_find_config` should return `@Value` usage locations per the plan.

Evidence:
- `tool-spring_find_config.json` for `spring.application.name` returned `application.yml:2`, not the Java `@Value` use.
- `criteria-session-stdout.json` entry `v1-7-config-password` returned only config-file location `application.yml:3`.
- Source contains `@Value("${spring.application.name}")` in `examples/springcloud-demo/src/main/java/com/example/config/UserCacheJob.java:9`.

Conclusion: configuration keys are indexed, but reverse lookup to code usage / `@Value` location is not implemented.
