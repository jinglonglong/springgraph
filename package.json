{
  "name": "@jinglonglong/springgraph",
  "version": "1.0.1",
  "description": "为 Spring Cloud 微服务架构量身打造的语义知识图谱与 MCP 服务器",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "springgraph": "./dist/bin/springgraph.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jinglonglong/springgraph.git"
  },
  "bugs": {
    "url": "https://github.com/jinglonglong/springgraph/issues"
  },
  "homepage": "https://github.com/jinglonglong/springgraph#readme",
  "files": [
    "dist",
    "scripts",
    "README.md"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc && npm run copy-assets && node -e \"require('fs').chmodSync('dist/bin/springgraph.js', 0o755)\"",
    "preuninstall": "node dist/bin/uninstall.js",
    "copy-assets": "node -e \"const fs=require('fs'),path=require('path');fs.mkdirSync('dist/db',{recursive:true});fs.copyFileSync('src/db/schema.sql','dist/db/schema.sql');fs.mkdirSync('dist/extraction/wasm',{recursive:true});fs.readdirSync('src/extraction/wasm').filter(f=>f.endsWith('.wasm')).forEach(f=>fs.copyFileSync('src/extraction/wasm/'+f,'dist/extraction/wasm/'+f));const copyDir=(src,dst)=>{if(!fs.existsSync(src))return;fs.mkdirSync(dst,{recursive:true});for(const e of fs.readdirSync(src,{withFileTypes:true})){const s=path.join(src,e.name),d=path.join(dst,e.name);if(e.isDirectory())copyDir(s,d);else fs.copyFileSync(s,d);}};copyDir('src/web/public','dist/web/public')\"",
    "dev": "tsc --watch",
    "cli": "npm run build && node dist/bin/springgraph.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:eval": "vitest run __tests__/evaluation/",
    "eval": "npm run build && npx tsx __tests__/evaluation/runner.ts",
    "clean": "node -e \"const fs=require('fs'),path=require('path');fs.rmSync('dist',{recursive:true,force:true});const rmBuildInfo=(d)=>{if(!fs.existsSync(d))return;for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory()){if(e.name!=='node_modules')rmBuildInfo(p);}else if(e.name.endsWith('.tsbuildinfo'))fs.rmSync(p,{force:true});}};rmBuildInfo('.');\"",
    "build:springgraph": "tsc -b packages/springkg-shared packages/springkg-core packages/springkg-installer",
    "test:springgraph": "vitest run __tests__/team-a"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "code-intelligence",
    "knowledge-graph",
    "static-analysis"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "commander": "^14.0.2",
    "cytoscape-dagre": "^4.0.0",
    "fast-string-width": "^3.0.2",
    "fast-wrap-ansi": "^0.2.0",
    "ignore": "^7.0.5",
    "jsonc-parser": "^3.3.1",
    "picomatch": "^4.0.3",
    "sisteransi": "^1.0.5",
    "tree-sitter-wasms": "^0.1.11",
    "web-tree-sitter": "^0.25.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.19.30",
    "@types/picomatch": "^4.0.2",
    "typescript": "^5.0.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.0.0 <25.0.0"
  }
}
