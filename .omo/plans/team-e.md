# Team E 实施计划 
MCP Tools + CLI## TL;DR> **Quick Summary**: 实现独立 MCP server (stdio) + 15 �?`spring_*` 工具 + 6 �?CLI 命令 + `server-instructions.ts`。Team E **只读** `springkg.db`，不�?symbols/edges；所有数据来�?Team B (Semantic)、C (Data)、D (Runtime)、F (Community)�?>> **Deliverables**:> - 1 个独�?MCP server (`packages/springkg-mcp/`) 
15 spring_* 工具> - 1 �?CLI (`packages/springkg-cli/`) 
6 个命�?> - 1 �?server-instructions.ts（~200 行，agent-facing 引导�?> - 每个工具至少 1 个单测（vitest�?>> **依赖关系**:> - Phase E1: Team A (SpringKg class) + Team B (FeignResolver) + Team D (ConfigResolver) 
Sprint 1> - Phase E2: Team C (MyBatis) + Team D (Nacos/Gateway) 
Sprint 2-3> - Phase E3: Team F (Community/Summary) 
Sprint 4>> **关键约束**: 不修�?`spring_symbols` / `spring_edges` 表；只通过 Team A 暴露�?`SpringKg.query()` / `SpringKg.execute()` �?---## Context### Original Request�?plan (`springcloud.md`) �?Team E 范围已明确定义：- **目标**�?4 �?`spring_*` MCP 工具 + 独立 MCP server + 6 �?CLI 命令 + server-instructions.ts- **位置**：`packages/springkg-mcp/src/**` + `packages/springkg-cli/src/**`- **不写�?*：read-only consumer of springkg.db### CodeGraph 上游参�?`packages/codegraph/src/mcp/` 是参考实�?
15 spring_* 工具需要在交互风格、错误处理、输出格式上对齐�?| 上游工具 | spring_* 对应 | 差异�?||---------|---------------|--------|| `codegraph_explore` | `spring_trace_flow` | spring_ �?HANDLED_BY/CALLS/EXECUTES_SQL/READS_TABLE �?|| `codegraph_node` | `spring_find_entry` / `spring_find_feign` / `spring_find_mapper` | �?spring_ 实体查询 || `codegraph_search` | `spring_search_feature` | �?feature_communities || `codegraph_impact` | `spring_method_impact` / `spring_field_impact` | 5 sections 而非 blast radius || `codegraph_files` | `spring_assets_overview` | service/middleware/config 概览 || `codegraph_status` | (无对�?
�?springkg status) | 状态独�?|**关键差异**: spring_ 工具必须�?**sectioned output** (1-5 sections per response)，每�?section 都有 markdown header，agent 通过 header 跳读�?### Research Findings- **MCP SDK**: �?`@modelcontextprotocol/sdk`（与上游 codegraph MCP server 同款�?- **stdio transport**: `StdioServerTransport` 启动；`process.stdin` 监听 JSON-RPC- **server-instructions.ts**: 通过 MCP `initialize` response �?`instructions` 字段返回 
单源引导（与上游对齐�?- **错误处理**: �?CLAUDE.md 一�?
`isError: true` 仅用�?genuine stop-trying 情况（path refusal）；spring_ 工具�?"not found" 返回 SUCCESS + guidance text- **敏感数据**: `is_sensitive=true` �?config property 只返�?key + file:line + is_sensitive�?*永不返回 value**---## Work Objectives### Core Objective实现 15 �?`spring_*` MCP 工具 + 6 �?CLI 命令，让 AI agent 通过 stdio MCP 协议查询 SpringCloud 微服务图谱：1. 入口查询（find_entry / find_feign / find_mapper / find_config�?2. 链路追踪（trace_flow�?3. 资产总览（assets_overview / nacos_overview / gateway_route�?4. 影响分析（method_impact / field_impact / runtime_dependency / env_diff�?5. 社区与变更面（search_feature / module_summary / find_change_surface�?### Concrete Deliverables| 类别 | 文件 | 数量 ||------|------|------|| MCP tools | `packages/springkg-mcp/src/tools/*.ts` | 15 || MCP server | `packages/springkg-mcp/src/server.ts` | 1 || MCP init | `packages/springkg-mcp/src/server-instructions.ts` | 1 || MCP entry | `packages/springkg-mcp/src/index.ts` | 1 || CLI entry | `packages/springkg-cli/src/index.ts` | 1 || CLI commands | `packages/springkg-cli/src/commands/*.ts` | 6 || Tests | `packages/springkg-mcp/__tests__/*.test.ts` | 14+ || Tests | `packages/springkg-cli/__tests__/*.test.ts` | 6+ || Total files | | ~40 |### Definition of Done- [ ] 15 spring_* 工具全部实现 + 单测通过- [ ] 独立 MCP server 通过 `echo '{"jsonrpc":"2.0","method":"tools/list","id":1}' | npx springkg-mcp` 返回所�?15 个工�?- [ ] `server-instructions.ts` 写入 MCP `initialize` response- [ ] 6 �?CLI 命令可用（init / index / status / inspect / watch / rebuild-community / uninit 
实际 6 个顶层命令，其中 inspect �?4 个子命令�?- [ ] **绝对不写** `spring_symbols` / `spring_edges` 表（grep 验证�?- [ ] **绝对不读** 配置明文（grep 验证：无 `value` 字段返回�?is_sensitive=true �?config�?- [ ] 所�?tool 输出经过 `validateOutput()` 走脱敏路�?- [ ] 错误处理�?CLAUDE.md 一致：�?path refusal �?`isError: true`- [ ] `npm test` �?`packages/springkg-mcp` �?`packages/springkg-cli` 全绿### Must Have- 15 spring_* 工具�?+4+6 分布：Sprint 1 / 2-3 / 4�?- 独立 stdio MCP server（不复用 codegraph 上游�?- server-instructions.ts 单源引导- 6 �?CLI 命令- 每个工具 �? �?vitest 单测- read-only 访问 springkg.db- 敏感配置永远脱敏- 错误返回 SUCCESS 形态携带引导（与上游对齐）### Must NOT Have (Guardrails)- �?任何�?`spring_symbols` / `spring_edges` �?INSERT / UPDATE / DELETE- �?任何�?`.codegraph/codegraph.db` 的写访问- �?任何�?`packages/codegraph/src/**` 的修�?- �?复写 Team B/C/D/F 已经实现的查�?
�?SpringKg 暴露�?query API- �?AI slop：每工具过度抽象、force-unwraps、冗�?`as any`- �?工具 description �?"you should" / "please" / imperative 
只描�?capability- �?任何配置明文返回（包�?stack trace 中的 password hint�?- �?阻塞主线程的同步 IO（所有查询走 prepared statement�?- �?�?server-instructions.ts 中写"请优先使�?（按 CLAUDE.md，禁�?steering�?- �?任何 hot-path `JSON.parse` 失败�?silent fallback（应 throw / return error result�?### Spec Framework Integration- **Detected Framework**: None（不�?webapp / TUI；纯 CLI/MCP backend�?- N/A 
不触�?frontend / visual-qa skills---## Team Interface Contracts### 文件所有权（关键！禁止越权�?| 路径前缀 | 拥有 | 备注 ||---------|------|------|| `packages/springkg-mcp/src/**` | **Team E** | MCP server + 15 tools || `packages/springkg-cli/src/**` | **Team E** | CLI 6 commands || `packages/springkg-mcp/__tests__/**` | **Team E** | tool 单测 || `packages/springkg-cli/__tests__/**` | **Team E** | CLI 单测 || `packages/springkg-mcp/package.json` | **Team E** | 依赖 `@modelcontextprotocol/sdk` || `packages/springkg-cli/package.json` | **Team E** | 依赖 `commander` |**禁止修改**: `packages/springkg-core/**`、`packages/springkg-semantic/**`、`packages/springkg-data/**`、`packages/springkg-runtime/**`、`packages/springkg-community/**`、`packages/springkg-shared/**`（除非要新增共享类型 
�?PR review�?### 输入契约（consumed�?Team E 通过 `SpringKg` class 的公�?API 读库（Team A 拥有 class 本身）：```typescript// Team A 暴露，Team E 消费import { SpringKg } from 'springkg-core';const springkg = await SpringKg.open({ projectPath: process.env.SPRINGKG_PROJECT_PATH });// �?symbols（Team B/C/D/F 写入�?await springkg.querySymbols({  kind: 'endpoint',                           // SpringKgNodeKind 字符�?  where: { url: '/api/users' },               // 任意字段  limit: 10,});await springkg.querySymbols({  kind: 'feign_client',  where: { name: 'UserServiceClient' },});// �?edgesawait springkg.queryEdges({  sourceId: 'endpoint:abc',  kind: 'HANDLED_BY',                         // SpringKgEdgeKind 字符�?  direction: 'out',});await springkg.queryEdges({  targetId: 'controller:xyz',  kind: 'CALLS',  direction: 'in',});// �?community（Team F 写入�?await springkg.queryCommunities({  keyword: '订单',                            // 中文/英文模糊  limit: 5,});await springkg.getCommunity(communityId);await springkg.getCommunitySummary(communityId);// 配置查询（Team D 写入�?await springkg.queryConfigs({  key: 'spring.datasource.url',  profile: 'prod',});```**依赖矩阵**:| Team E Task | 依赖�?Resolver/Builder | 来自 ||------------|----------------------|------|| T10 (CLI init/index/status) | SpringKg class | Team A (T1-T9) || T17 (MCP server) | SpringKg class | Team A || T18 (find_entry) | EndpointResolver + FeignResolver | Team B (T41, T42) || T19 (find_feign) | FeignResolver | Team B (T42) || T20 (assets_overview) | ConfigResolver + MiddlewareInventory | Team D (T37, T38) || T21 (trace_flow initial) | EndpointResolver + Service/Repository extraction | Team B (T39-T42) || T33 (find_mapper) | MyBatisXmlExtractor + SqlTableExtractor | Team C (T25-T30) || T34 (trace_flow extended) | �?T21 + MyBatis | Team C || T44 (find_config) | ConfigResolver | Team D (T38) || T45 (nacos/gateway) | NacosConfigResolver + GatewayRouteResolver | Team D (T46, T47) || T51 (search_feature) | CommunityBuilder | Team F (T47) || T52 (method/field impact) | 全部 resolvers | Team B+C+D || T53 (module_summary) | SummaryGenerator | Team F (T50) || T54 (find_change_surface) | CommunityBuilder + FeatureMatcher | Team F (T47, T58) || T55 (runtime_dependency) | Nacos + MQ + DB extractors | Team B+D || T56 (env_diff) | ConfigResolver (multi-profile) | Team D (T38) |### 输出契约（produced�?**15 spring_* MCP 工具**:| # | Tool name | Phase | Sprint | 描述 ||---|-----------|-------|--------|------|| 1 | `spring_find_entry` | E1 | 1 | 入口查询（URL/Controller/Feign/MQ/Scheduled�?|| 2 | `spring_find_feign` | E1 | 1 | Feign 客户端查�?|| 3 | `spring_assets_overview` | E1 | 1 | 服务/中间�?敏感配置总览 || 4 | `spring_trace_flow` | E1+E2 | 1,2 | 调用链追踪（深度 1-5�?|| 5 | `spring_find_mapper` | E2 | 2 | MyBatis Mapper / SQL / Table 查询 || 6 | `spring_find_config` | E2 | 3 | ConfigProperty 反查 || 7 | `spring_nacos_overview` | E2 | 3 | Nacos 集群/命名空间总览 || 8 | `spring_gateway_route` | E2 | 3 | Gateway 路由查询 || 9 | `spring_search_feature` | E3 | 4 | 功能社区关键词搜�?|| 10 | `spring_method_impact` | E3 | 4 | 方法级影响分析（4+ sections�?|| 11 | `spring_field_impact` | E3 | 4 | 字段级影响分析（2+ sections�?|| 12 | `spring_module_summary` | E3 | 4 | 社区摘要输出 || 13 | `spring_find_change_surface` | E3 | 4 | 自然语言变更面分�?|| 15 | `spring_runtime_dependency` | E3 | 4 | 运行时依赖（DB/Redis/MQ/Feign/Config�?|| 15 | `spring_env_diff` | E3 | 4 | 跨环境配置对�?|> 实际 15 个工�?+ `spring_assets_overview` 拆分 = **15 �?*，但设计上是 15 �?tool。`spring_assets_overview` �?tool 输出 3 sections�?**6 �?CLI 命令**:| # | Command | Phase | 说明 ||---|---------|-------|------|| 1 | `springkg init [path]` | E1 | 初始�?.codegraph/springkg.db || 2 | `springkg index [path]` | E1 | 全量索引 || 3 | `springkg status [path]` | E1 | 显示统计 || 4 | `springkg inspect <kind> <arg>` | E1 | 子命�? endpoint/feign/mapper/config || 5 | `springkg watch [path]` | E1 | 文件监听 + 增量同步 || 6 | `springkg rebuild-community` | E1 | 重建 feature_communities || 7 | `springkg uninit [path]` | E1 | 删除 .codegraph/springkg.db |实际命令�?= 7（含 uninit），�?plan 写的 "6 �?是初版口�?
以本 plan 为准�?---## Cross-team Sync Points### Sync E1.0 
Team A Phase 1 完成 (Blocking)- **触发条件**: `new SpringKg({ projectPath }).init()` 跑�?+ 8 张表 schema 部署- **产出**:  - `SpringKg.open()` / `SpringKg.close()` / `SpringKg.querySymbols()` / `SpringKg.queryEdges()` API 稳定  - 8 张表全部存在 (spring_symbols, spring_edges, spring_endpoints, spring_feign_clients, spring_sql_statements, runtime_config_properties, feature_communities, feature_community_members)- **E 启动条件**: SpringKg class + 至少 1 �?query API 公开- **E 阻塞�?*: ~2-3 天（Team A 完成后立刻开工）### Sync E1.1 
Team B FeignResolver 雏形 (Phase E1 启动)- **触发条件**: Team B 提交 `packages/springkg-semantic/src/feign-resolver.ts` 雏形（至少能�?1 �?feign_client symbol�?- **E 需要的输出**:  - `endpoint` symbols（`url` / `http_method` / `controller_class` 字段填好�?  - `feign_client` symbols（`name` / `target_service_name` 字段�?  - `feign_method` symbols（`name` / `return_type` / `path` 字段�?  - edges: `HANDLED_BY`, `CALLS_FEIGN`, `TARGETS_ENDPOINT`- **E 依赖�?task**: T18, T19, T21 雏形- **同步会议**: Team B 完成 Phase B1 (Sprint 1) 后通知 E### Sync E1.2 
Team D ConfigResolver 雏形 (Phase E1 启动)- **触发条件**: Team D 提交 `packages/springkg-runtime/src/config-resolver.ts`- **E 需要的输出**:  - `config_property` symbols（`key` / `value_hash` / `is_sensitive` / `profile` / `file:line`�?  - `middleware` symbols（`type` / `host` / `port`�?  - edges: `USED_BY`, `CONNECTS_TO`- **E 依赖�?task**: T20, T44, T56- **同步会议**: Team D Phase D1 完成后通知 E### Sync E2.1 
Team C MyBatis 完整 (Phase E2 启动)- **触发条件**: Team C 完成 mybatis-xml-extractor + sql-table-extractor- **E 需要的输出**:  - `mapper` symbols（`namespace` / `interface_name` 字段�?  - `mapper_method` symbols（`name` / `result_type` 字段�?  - `sql_statement` symbols（`sql_text` / `param_type` 字段�?  - `table` symbols（`name` 字段�?  - edges: `EXECUTES_SQL`, `READS_TABLE`, `WRITES_TABLE`, `MAPS_TO_TABLE`, `BIND_TO`- **E 依赖�?task**: T33, T34- **同步会议**: Team C Sprint 2 完成后通知 E### Sync E2.2 
Team D Nacos/Gateway 完整 (Phase E2 启动)- **触发条件**: Team D 完成 nacos-config-resolver + gateway-route-resolver- **E 需要的输出**:  - `nacos_cluster` symbols（`address` / `namespace` 字段�?  - `nacos_config` symbols（`data_id` / `group` / `profile` 字段�?  - `gateway_route` symbols（`path` / `target_service` / `predicates` 字段�?  - edges: `LOADS_CONFIG`, `ROUTES_TO`- **E 依赖�?task**: T45### Sync E3.1 
Team F Community 完整 (Phase E3 启动)- **触发条件**: Team F 完成 community-builder + summary-generator + dirty-queue- **E 需要的输出**:  - `feature_community` symbols（`name` / `keywords` / `summary` 字段�?  - `feature_community_members` rows（`community_id` / `codegraph_node_id` 字段�?  - `getCommunitySummary(communityId)` 返回结构�?markdown  - `matchCommunities({ requirement: "..." })` 返回 related community ids- **E 依赖�?task**: T51, T53, T54### Sync 收口 
Team G 集成测试 (Phase E3 �?- **触发条件**: Team E Phase E3 完成 + Team F 完成 + Team G 集成测试启动- **E 配合**: �?bug + 调优输出（output budget�?- **Team G 验证**: 10 �?V1 criteria---## Task List (20 total 
3 phases)### Phase E1: Sprint 1 MCP infrastructure + 4 tools> **依赖**: Team A Phase 1（SpringKg class + schema�? Team B FeignResolver 雏形 + Team D ConfigResolver 雏形> **目标**: 独立 MCP server 跑起�?+ 4 �?spring_* 工具可调 + server-instructions 引导完整> **Sprint 验收**: `echo '{"jsonrpc":"2.0","method":"tools/list","id":1}' | npx springkg-mcp` 返回 4 个工具；4 个工具对 demo 项目返回真实数据- [x] 1. [E] **[T10]** `packages/springkg-cli/src/index.ts`
springkg CLI with commander. Commands: `init`, `index`, `status` (initial; others added by T61/T62/T64). Why: �?agent �?developer 通过 CLI 验证 springkg.db 状态。Expected: `springkg --help` 显示 7 个顶层命�?+ 每个子命令可独立调通�? 个单�? `init` 创建 .codegraph/springkg.db；`index` 输出进度；`status` 显示 N symbols/M edges�?- [x] 2. [E] **[T17]** `packages/springkg-mcp/src/server.ts`
独立 MCP server using StdioServerTransport. Read `SPRINGKG_PROJECT_PATH` env var. Tools registry. `tools/list` and `tools/call` dispatch. Why: agent 通过 stdio �?spring_ 工具的唯一入口；必须独立不依赖 codegraph 上游 MCP。Expected: stdin �?JSON-RPC 2.0；正确响�?`tools/list` �?`tools/call`；未初始化返�?`isError: false` + 引导�? 个单�? tools/list 数量；tools/call 成功路径；tools/call 失败返回 SUCCESS+guidance（不�?isError）�?- [x] 3. [E] **[T18]** `packages/springkg-mcp/src/tools/find-entry.ts`
`spring_find_entry`. Input: `{ url?: string, controller?: string, method?: string, feign_name?: string, mq?: string, scheduled?: string }`. Query spring_endpoints by URL �?fuzzy match by controller class name �?query Feign by name �?MQ consumer/Scheduled task. Output: endpoint + handler file:line + call chain head. Why: 4 类入口（HTTP/Feign/MQ/Scheduled）统一查询。Expected: 4 �?case (URL 命中 / controller 模糊 / Feign name / MQ topic)�? 个单�?(1 per case)�?- [x] 4. [E] **[T19]** `packages/springkg-mcp/src/tools/find-feign.ts`
`spring_find_feign`. Input: `{ name?: string, interface?: string, target_service?: string }`. Query spring_feign_clients + spring_feign_methods. Output: feign client + methods[] + target_service_name + (optional) target_endpoint (if bridge 解析成功). Why: Feign 客户端是 SpringCloud 微服务调用关键入口。Expected: 2 �?case (name 命中 / target_service 反查)�? 个单测�?- [x] 5. [E] **[T20]** `packages/springkg-mcp/src/tools/assets-overview.ts`
`spring_assets_overview`. Output: 3 sections 
`## Services`（name + port�? `## Middlewares`（type + host + port�? `## Sensitive Config`（key + is_sensitive=true 的项�?*�?value**）。Why: 资产总览 
4 �?V1 criteria 涉及。Expected: 2 �?case (positive 
有数�?/ sensitive leak test 
验证无明�?password/secret/token)�? 个单测�?- [x] 6. [E] **[T21]** `packages/springkg-mcp/src/tools/trace-flow.ts`
`spring_trace_flow` (initial, depth 1-3). Input: `{ url: string, depth?: number, direction?: 'down'|'up'|'both' }`. From spring_endpoints follow HANDLED_BY �?CALLS �?CALLED_BY �?handler �?service �?repository. Output hierarchical: `## Endpoint` �?`## Controller` �?`## Service` �?`## Repository` (depth=3 returns 4 levels). Why: 调用链是 trace 查询基础。Expected: 2 �?case (depth=1 端点+handler；depth=2 �?service)�? 个单测�?- [x] 7. [E] **[T60 Metis C3]** `packages/springkg-mcp/src/server-instructions.ts`
Agent-facing instructions. 1-2 �?per spring_* tool. ~200 �?max. Used in MCP `initialize` response. **CRITICAL**: 没有这个，agent 不会�?spring_ 工具（与上游一�?
CLAUDE.md 强调）。Why: server-instructions 是单源引导；禁止�?15 个工�?description 里写"please use this tool"�?steering。Expected: 15 个工具每�?1-2 句描述能力；含工作流 (find_entry �?trace_flow �?method_impact)；含 NOT-INDEXED-WORKSPACE 引导�? 个单�?(line count < 220 + 15 个工具名都在)�?- [x] 8. [E] **[T61 Metis C4]** `springkg watch [path]` CLI
`packages/springkg-cli/src/commands/watch.ts`. Wraps `SpringKg.watch()`. SIGINT handler for graceful shutdown (close DB, log "watch stopped"). Why: developer 调试时实时同步图谱。Expected: 1 �?case (启动 watch + 改文�?+ 验证 sync 触发)�? 个单�?+ 1 �?e2e (subprocess)�?- [x] 9. [E] **[T62 Metis C4]** `springkg inspect` subcommand
`packages/springkg-cli/src/commands/inspect.ts`. 子命�? `endpoint <url>` / `feign <name>` / `mapper <namespace>` / `config <key>`. 复用对应 spring_* 工具的内部实现。Why: �?developer 提供一个终�?query 入口；agent 优先�?MCP。Expected: 4 �?case (每个子命令一�?�? 个单测�?- [x] 10. [E] **[T64 Metis C4]** `springkg rebuild-community` + `springkg uninit` CLI
`packages/springkg-cli/src/commands/rebuild-community.ts` + `uninit.ts`. rebuild-community: 触发 `SpringKg.rebuildCommunity()` (F �?CommunityBuilder + SummaryGenerator). uninit: 删除 `.codegraph/springkg.db`�?*不删** `.codegraph/codegraph.db`）。Why: 重建社区是更�?feature_communities 的入口；uninit �?clean uninstall。Expected: 2 �?case (rebuild 写入新社区；uninit 只删 springkg.db 保留 codegraph.db)�? 个单测�?### Phase E2: Sprint 2-3 additional tools> **依赖**: Phase E1 完成 + Team C MyBatis 完整 + Team D Nacos/Gateway 完整> **目标**: 8 个工具全部可调（4 from E1 + 4 from E2�?> **Sprint 验收**: trace_flow depth=5 走�?Endpoint→Controller→Service→Mapper→SQL→Table- [ ] 11. [E] **[T33]** `packages/springkg-mcp/src/tools/find-mapper.ts` 
`spring_find_mapper`. Input: `{ namespace?: string, method?: string, table?: string, sql?: string }`. Query spring_sql_statements + spring_mappers. Output: `## Mapper` (interface name) + `## XML Statement` (id + file:line) + `## SQL Preview` (first 200 chars) + `## Tables` (table names) + `## Bound Method` (Service method that calls it). Why: MyBatis �?Java/Spring 数据访问核心；agent �?SQL 前要看到 mapper。Expected: 4 �?case (namespace 命中 / method 命中 / table 反查 / SQL 模糊)�? 个单测�?- [ ] 12. [E] **[T34]** 扩展 `spring_trace_flow` 
extend `packages/springkg-mcp/src/tools/trace-flow.ts`. depth=5 returns 6 levels: Endpoint �?Controller �?Service �?Mapper �?SQL �?Table. Each level: file:line + name + (table only) column hint. Why: V1 §1 验证条件。Expected: 1 �?case (depth=5 完整 6 �?�? 个单测�?- [ ] 13. [E] **[T44]** `packages/springkg-mcp/src/tools/find-config.ts` 
`spring_find_config`. Input: `{ key: string, profile?: string }`. Output: `## Definition` (file:line) + `## Profile` (matched profile) + `## Is Sensitive` (bool) + `## Used By` (Service methods reading this key). **如果 is_sensitive=true，绝不返�?value 字段，只返回 hash 引用**。Why: 改配置前要看影响面；V1 §7 验证条件。Expected: 1 �?case (positive 
�?USED_BY �?�? 个单�?+ 1 个安全单�?(sensitive 不返�?value)�?- [ ] 14. [E] **[T45]** `packages/springkg-mcp/src/tools/nacos-overview.ts` + `gateway-route.ts` 
`spring_nacos_overview` + `spring_gateway_route`. nacos-overview: input `{}`, output `## Clusters` (address) + `## Namespaces` (namespace_id + name) + `## Data IDs` (data_id + group) + `## Services` (service_name). gateway-route: input `{ path?: string }`, output `## Routes` (path + target_service + predicates) + `## Target Services` (downstream service info). Why: Nacos 是配置中�?+ 服务发现；Gateway 是流量入口。Expected: 2 �?case (nacos 
4 sections；gateway 
path 命中 + 路由详情)�? 个单测�?### Phase E3: Sprint 4 community/impact tools> **依赖**: Phase E2 完成 + Team F CommunityBuilder + SummaryGenerator> **目标**: 15 个工具全实现 + 集成�?demo 项目> **Sprint 验收**: 15 工具全部�?demo 上能跑出真实数据；F1-F4 通过- [ ] 15. [E] **[T51]** `packages/springkg-mcp/src/tools/search-feature.ts` 
`spring_search_feature`. Input: `{ query: string, limit?: number }`. Query feature_communities.keywords (FTS5-like LIKE) + name fuzzy. Sort by keyword match count + summary length desc. Output top-N community summaries. Why: 功能社区�?V1 §8 验证；agent 用自然语言�?订单模块"。Expected: 1 �?case (`query: "订单"` �?订单社区排第 1)�? 个单测�?- [ ] 16. [E] **[T52]** `packages/springkg-mcp/src/tools/method-impact.ts` + `field-impact.ts` 
`spring_method_impact` + `spring_field_impact`. method_impact: input `{ method_id: string }`, output 5 sections 
`## Callers` (upstream) + `## Endpoints` (HTTP entry points) + `## Feign Callers` (cross-service) + `## SQL Operations` (downstream DB) + `## Config Dependencies` (env vars used) + `## Transactions` (if @Transactional found) + `## MapStruct Mappings` (if entity mapping found). field_impact: input `{ field_id: string }`, output 
`## Mapped Fields` (DTO/VO) + `## Used In` (read/write sites) + `## MapStruct Sources` (cross-class mapping). Why: 影响分析�?change-driven 开发核心。Expected: 2 �?case (method_impact �? sections；field_impact �? sections)�? 个单测�?- [ ] 17. [E] **[T53]** `packages/springkg-mcp/src/tools/module-summary.ts` 
`spring_module_summary`. Input: `{ community_id?: string, name?: string }`. Output: full summary (SummaryGenerator format 
markdown with `## 概述` / `## 主要类` / `## 入口` / `## 数据表` / `## 配置依赖` / `## 外部依赖` / `## 调用关系`). Why: agent 接手不熟悉的模块前要"鸟瞰"。Expected: 1 �?case (community_id 命中返回完整 6 sections)�? 个单测�?- [ ] 18. [E] **[T54]** `packages/springkg-mcp/src/tools/find-change-surface.ts` 
`spring_find_change_surface`. Input: `{ requirement: string }` (自然语言). Match communities (Team F �?`matchCommunities` 暴露). Output: `## 受影响入口` (related endpoint symbols) + `## 建议修改面` (related classes/methods sorted by community membership count) + `## 配置变更` (config properties read by related symbols) + `## 跨服务影响` (Feign call targets). Why: 需求分析第一�?
�?修改 X 模块"开清单。Expected: 1 �?case (`requirement: "改订单状态机"` �?订单社区命中 + 修改�?�? 个单测�?- [ ] 19. [E] **[T55]** `packages/springkg-mcp/src/tools/runtime-dependency.ts` 
`spring_runtime_dependency`. Input: `{ method_id: string }`. Trace downstream: `## Database` (SQL/table ops) + `## Redis` (cache ops if detectable) + `## MQ` (Kafka/RabbitMQ send/recv) + `## Feign` (cross-service calls) + `## Nacos Config` (env vars read). Why: 部署/排障前要知道"这个方法依赖什�?。Expected: 1 �?case (5 sections 至少 3 非空)�? 个单测�?- [ ] 20. [E] **[T56]** `packages/springkg-mcp/src/tools/env-diff.ts` 
`spring_env_diff`. Input: `{ key: string }`. Query all profiles in runtime_config_properties. Output: `## Per-Profile Values` (table: profile | value | is_sensitive; sensitive �?value �?masked as `***`). Why: 跨环境配置核对。Expected: 1 �?case (key 命中�?profile �?表格输出 + sensitive 脱敏)�? 个单�?+ 1 个安全单测�?---## Implementation Notes (per Task 
pitfalls + decisions)### T10 
CLI init/index/status**Pitfall**: commander 默认 exit-on-error 会吞 stack trace；显�?`program.exitOverride()`�?**Decision**: init 不调 index 
�?user 显式�?`springkg index` (�?codegraph 上游行为一�?�?### T17 
MCP server**Pitfall**: `process.stdin` JSON-RPC 必须�?line-delimited JSON；MCP SDK 已经处理但要 verify�?**Decision**: tools/call 失败路径必须返回 `isError: false` + 引导 text（per CLAUDE.md）�?### T18 
find_entry**Pitfall**: Feign 字段�?`target_service_name` vs `targetServiceName` 
�?snake_case（与 SQL 表一致）�?**Decision**: 4 类入口共用同一 tool；input 字段 4 �?1，命中即返回�?### T19 
find_feign**Pitfall**: Feign 跨服务目�?`target_endpoint` 来自 `TARGETS_ENDPOINT` edge；只�?FeignProviderBridge 解析成功时返回�?**Decision**: `target_endpoint` 字段 optional；缺失时显示"not bridged"�?### T20 
assets_overview**Pitfall**: 中间�?host/port 可能�?`${VAR}` 占位�?
�?`value_hash` 引用而不�?expand�?**Decision**: Sensitive section 只列 key + file:line + is_sensitive，无 value 字段�?### T21 
trace_flow initial**Pitfall**: depth 边界 
depth=0 �?endpoint 自身；depth=1 �?handler；depth=2 �?service�?**Decision**: �?BFS 控制 depth，不递归避免 stack overflow�?### T60 
server-instructions.ts**Pitfall**: 长度�?220 行会�?MCP SDK truncate。CLAUDE.md 强调"single source of truth"�?**Decision**: 15 工具每个 1-2 句；工作流用 bullet；不�?imperative ("you should")�?### T61 
CLI watch**Pitfall**: SIGINT �?Windows 上是 `SIGBREAK` 
监听两个 signal�?**Decision**: graceful shutdown 
close DB �?exit 0；不退避�?### T62 
CLI inspect**Pitfall**: `inspect endpoint /api/x` �?`inspect feign X` parser 不同 
�?commander subCommand�?**Decision**: 4 个子命令各自独立 file（`commands/inspect-{endpoint,feign,mapper,config}.ts`）�?### T64 
CLI rebuild-community + uninit**Pitfall**: uninit 误删 codegraph.db 
显式只删 springkg.db�?**Decision**: rebuild-community 同步触发 SummaryGenerator 重建；异步可后续优化�?### T33 
find_mapper**Pitfall**: SQL 文本长（>200 chars）
truncate �?"..."�?**Decision**: tables 数组去重；bound_method 是反向查询（Service.method �?Mapper）�?### T34 
trace_flow extended**Pitfall**: Mapper 节点可能多个（interface + xml）
选最近的 1 个�?**Decision**: depth=5 输出 6 levels；column 只在 table level 显示 first 3 cols�?### T44 
find_config**Pitfall**: sensitive 字段误返�?value 
�?assert 在单测�?**Decision**: `is_sensitive=true` �?output schema 不含 value 字段（TS 编译期就限制）�?### T45 
nacos/gateway**Pitfall**: Nacos dataId 命名规则不统一 
�?group 分组�?**Decision**: 2 个独�?tool 而非 1 个（nacos vs gateway 域不同）�?### T51 
search_feature**Pitfall**: 中文 keyword LIKE �?UTF-8 编码 
SQLite 默认 OK，但 pg_trgm 类似需要确认�?**Decision**: 排序 `keyword_match_count DESC, summary_length DESC`；limit 默认 5�?### T52 
method/field impact**Pitfall**: method_impact section �?4+ 
6-7 sections 全填�?**Decision**: 字段影响�?2+ sections（按需�?2 即可）�?### T53 
module_summary**Pitfall**: SummaryGenerator 输出格式不固�?
�?F 提供 schema 文档，E �?schema 渲染�?**Decision**: 6 sections 固定（概�?主要�?入口/数据�?配置依赖/外部依赖/调用关系）�?### T54 
find_change_surface**Pitfall**: 自然语言匹配 
�?F �?`matchCommunities({requirement})`�?**Decision**: 4 sections（受影响入口/建议修改�?配置变更/跨服务影响）�?### T55 
runtime_dependency**Pitfall**: Redis/MQ 不是所有项目都�?
5 sections 允许 0 命中�?**Decision**: 至少 1 个非�?section 才能返回；全空时�?无运行时依赖"�?### T56 
env_diff**Pitfall**: sensitive 脱敏在渲染时做，不是 query �?
渲染层强制�?**Decision**: 表格输出；sensitive 行的 value 列永�?`***`（即使是 dev profile）�?---## Verification Strategy### 单元测试（每个工具至�?1 个）```bash# Team E 单测npx vitest run packages/springkg-mcp/__tests__/npx vitest run packages/springkg-cli/__tests__/# 覆盖率门�?npx vitest run --coverage packages/springkg-mcp/ packages/springkg-cli/# 目标: lines > 70%, branches > 60%, functions > 80%```### 集成验证```bash# 1. CLI 端到�?cd examples/springcloud-demonpx springkg initnpx springkg indexnpx springkg status        # 期望: N symbols, M edgesnpx springkg inspect endpoint /api/usersnpx springkg watch &# 改文�?�?验证 auto-sync# 2. MCP 端到�?echo '{"jsonrpc":"2.0","method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}},"id":1}' | SPRINGKG_PROJECT_PATH=examples/springcloud-demo npx springkg-mcpecho '{"jsonrpc":"2.0","method":"tools/list","id":2}' | SPRINGKG_PROJECT_PATH=examples/springcloud-demo npx springkg-mcp | jq '.result.tools | length'# 期望: 14# 3. 工具调�?echo '{"jsonrpc":"2.0","method":"tools/call","params":{"name":"spring_find_entry","arguments":{"url":"/api/users"}},"id":3}' | SPRINGKG_PROJECT_PATH=examples/springcloud-demo npx springkg-mcp```### 越权检�?```bash# 1. 检�?Team E 没写 spring_symbols/spring_edgesgrep -rE "(INSERT|UPDATE|DELETE).*spring_symbols|spring_edges" packages/springkg-mcp/src/ packages/springkg-cli/src/# 期望: 0 命中# 2. 检�?Team E 没碰 codegraph 上游git diff packages/codegraph/ | wc -l# 期望: 0# 3. 检�?sensitive 字段不返�?valuegrep -rE "value.*sensitive" packages/springkg-mcp/src/tools/find-config.ts packages/springkg-mcp/src/tools/env-diff.ts# 期望: 0 命中（值永远不返回�?```### V1 验证 (与主 plan 对齐)| V1 编号 | 验证方法 | E task ||---------|---------|--------|| V1 §1 | `spring_trace_flow({url:"/api/x", depth:5})` 输出 5 �?| T34 || V1 §2 | `spring_find_feign({name:"X"})` �?`target_endpoint` | T19 || V1 §3 | `spring_field_impact` 返回受影响字�?| T52 || V1 §4 | `spring_find_entry({mq:"topic"})` | T18 || V1 §5 | `spring_find_entry({scheduled:"*"})` | T18 || V1 §6 | `spring_method_impact` �?transaction section | T52 || V1 §7 | `spring_find_config` 返回 @Value 位置 | T44 || V1 §8 | `spring_search_feature({query:"订单"})` | T51 || V1 §9 | `spring_method_impact` 4+ sections | T52 || V1 §10 | `spring_field_impact` 2+ sections | T52 |---## Execution Strategy### Sub-agent 分配| Sub-task | Agent | 负载 ||----------|-------|------|| T10, T17, T60 | `deep` | 基础设施 (CLI 入口 + MCP server + server-instructions) || T18, T19, T20, T21 | `unspecified-high` × 2 | Phase E1 4 工具 (parallel) || T33, T34, T44, T45 | `unspecified-high` × 2 | Phase E2 4 工具 (parallel) || T51, T52, T53, T54, T55, T56 | `unspecified-high` × 3 | Phase E3 6 工具 (parallel) || T61, T62, T64 | `unspecified-high` × 1 | Phase E1 CLI 收尾 (parallel with 4 工具) |**�?agent �?*: 1 deep + 8 unspecified-high = 9 sub-tasks### Commit 策略每个 Phase 完成�?commit �?`team-e-mcp` 分支�?- `phase-e1: T10/T17/T18/T19/T20/T21/T60/T61/T62/T64 + 6 CLI + 4 tools + server-instructions`- `phase-e2: T33/T34/T44/T45 + 4 tools`- `phase-e3: T51/T52/T53/T54/T55/T56 + 6 tools`主分�?squash-merge �?phase（与�?plan 一致）�?### Risk & Mitigation| 风险 | 影响 | 缓解 ||------|------|------|| Team A 延迟 SpringKg API 公开 | Phase E1 整体延期 | 提前 review Team A 接口设计；准�?mock data || Team B FeignResolver �?| T18/T19 数据�?| mock fixtures 支持；单测不依赖真实数据 || Team F CommunityBuilder 雏形�?| Phase E3 整体延期 | Phase E1-E2 可独立完工；E3 �?F || 敏感数据泄露（误返回 value�?| **安全** | 单测 + grep 双重检查；TS type system �?find-config.ts / env-diff.ts 强制 value 字段�?sensitive 路径不存�?|| tool description �?steering 违反 CLAUDE.md | 工具�?under-pick | server-instructions.ts 是单源；tool description 只描�?capability || MCP SDK 版本不匹配上�?| server 启动失败 | 锁版本到�?codegraph 上游相同（@modelcontextprotocol/sdk@^0.5.0�?|---## Success Criteria### 完工 Checklist- [ ] 20 �?task 全部实现 + 单测通过- [ ] 15 spring_* 工具全部�?`tools/list` �?- [ ] `server-instructions.ts` �?220 �?+ 15 工具名全覆盖- [ ] 6+1 �?CLI 命令全部可用（init/index/status/inspect/watch/rebuild-community/uninit�?- [ ] 越权检�?0 命中（spring_symbols / spring_edges �?INSERT；codegraph/ �?diff�?- [ ] sensitive 字段检�?0 命中（无 value 明文返回�?- [ ] `npm test` 0 失败- [ ] V1 §1-§10 全部通过- [ ] F1-F4 final verification APPROVE### Final Verification Commands```bash# 单测npx vitest run packages/springkg-mcp packages/springkg-cli# 15 工具验证echo '{"jsonrpc":"2.0","method":"tools/list","id":1}' | SPRINGKG_PROJECT_PATH=examples/springcloud-demo npx springkg-mcp | jq '.result.tools | length'# 期望: 14# 越权检�?grep -rE "(INSERT|UPDATE|DELETE).*spring_(symbols|edges)" packages/springkg-mcp/src/ packages/springkg-cli/src/# 期望: �?# 敏感检�?grep -rE "is_sensitive.*value|value.*is_sensitive" packages/springkg-mcp/src/tools/# 期望: �?# CLI 端到�?cd examples/springcloud-demonpx springkg init && npx springkg index && npx springkg status# 期望: �?error；symbols/edges 计数 > 0```### Final Checklist (与主 plan 对齐)- [ ] Team E 全部 20 任务完成- [ ] 15 spring_* 工具 + server-instructions 完整- [ ] 6+1 CLI 命令全部可用- [ ] `packages/codegraph/` 未修�?- [ ] springkg.db �?`.codegraph/springkg.db`（Team A 拥有，E 间接�?- [ ] 敏感配置 0 明文- [ ] 越权写库 0 �?- [ ] F1-F4 全部 APPROVE- [ ] 用户明确�?okay"才算完成