# V1 §1 — Endpoint → Mapper → SQL → Table chain

Status: FAIL

Expected: `spring_trace_flow(url="/api/x", depth=5)` should show Endpoint → Controller → Service → Mapper → SQL → Table.

Evidence:
- `criteria-session-stdout.json` entry `v1-1-trace-orders` returned only 3 steps: endpoint, controller_method, service.
- `post-session-db-counts.json` shows `spring_sql_statements: 0`.
- `criteria-session-stdout.json` entries `integration-mapper-order` and `integration-mapper-user` both returned `found:false` with empty methods.

Observed output excerpt:
- `GET /api/orders/summary`
- `com.example.order.OrderController.summary`
- `com.example.order.OrderService.getOrderSummary`

Conclusion: the flow never reaches mapper, SQL, or table layers.
