# V1 §5 — `@Scheduled` task entry

Status: FAIL

Expected: `spring_find_entry({scheduled:"*"})` should return scheduled task entries.

Evidence:
- Demo source contains scheduled methods in `UserCacheJob.java:10` and `OrderController.java:20`.
- `criteria-session-stdout.json` entry `v1-5-scheduled` returned `OrderController.summary` instead of a scheduled task.
- `tool-list.json` / `server.ts` input schema for `spring_find_entry` has no `scheduled` argument.

Conclusion: scheduled tasks exist in source, but the advertised acceptance-style invocation is not supported by the implemented tool.
