# V1 §4 — MQ producer / consumer

Status: FAIL

Expected: `spring_find_entry({mq:"topic"})` should locate MQ producers/consumers.

Evidence:
- Demo source grep found no MQ annotations or producer APIs.
- `criteria-session-stdout.json` entry `v1-4-mq-topic` returned a normal controller result (`OrderController.summary`) instead of MQ-specific output, showing the server ignores the `mq` argument.

Conclusion: there is no demo MQ coverage, and the tool invocation does not implement MQ filtering semantics.
