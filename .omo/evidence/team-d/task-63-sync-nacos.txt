# Evidence: Task 63 - sync-nacos CLI

## Test Results
```
cd packages/springkg-runtime && npx vitest run sync-nacos --reporter=verbose

✓ case 1: runSyncNacos on fixture with bootstrap.yml (1 nacos_cluster, 1 nacos_config) -> scanned >= 1, added >= 2, errors = []
[springkg] sync-nacos scanned=1 added=3 updated=0 removed=0 duration=10ms

✓ dryRun=true should not persist but still report scanned count
[springkg] sync-nacos scanned=1 added=3 updated=0 removed=0 duration=2ms

2 passed | sync-nacos.test.ts
```

## TypeScript Compilation
```
cd packages/springkg-runtime && npx tsc --noEmit
(no output = success)
```

## Implementation Summary
- packages/springkg-runtime/src/sync-nacos.ts

## Key Features
- Thin wrapper around NacosConfigResolver.enhance()
- Supports --dry-run flag (no DB writes, reports counts)
- Reports scanned, added, updated, removed, duration metrics
- Logging format: [springkg] sync-nacos scanned=N added=N updated=N removed=N duration=NNNms
- Returns SyncNacosResult interface with all counts + duration
