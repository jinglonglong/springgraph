# V1 §8 — Feature community search

Status: FAIL

Expected: `spring_search_feature({query:"订单"})` should find the order-related feature community.

Evidence:
- `criteria-session-stdout.json` entry `v1-8-feature-chinese` returned `found:false`, `communities:[]`.
- `tool-spring_search_feature.json` with English query `order` did return communities, including `order-management`.

Conclusion: the acceptance query from the plan fails on the demo; only English label matching currently works.
