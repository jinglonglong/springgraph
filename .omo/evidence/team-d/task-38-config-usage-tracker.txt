# Evidence: Task 38 - ConfigPropertyUsageTracker

## Test Results
```
cd packages/springkg-runtime && npx vitest run config-usage-tracker --reporter=verbose

✓ case 1: UserService.java with @Value("${spring.datasource.url}") on field -> 1 USED_BY edge
✓ case 2: AppProperties.java with @ConfigurationProperties(prefix = "app.datasource") and runtime_config_properties has app.datasource.max-pool-size, app.datasource.min-idle -> 2 USED_BY edges

2 passed | config-usage-tracker.test.ts
```

## TypeScript Compilation
```
cd packages/springkg-runtime && npx tsc --noEmit
(no output = success)
```

## Implementation Summary
- packages/springkg-runtime/src/config-usage-tracker.ts

## Key Features
- Reads @Value("${...}") patterns from Java code via kg.codegraph.findNodes()
- Reads @ConfigurationProperties(prefix = "...") class-level annotations
- Writes USED_BY edges from config_property to class/method nodes
- Matches @Value keys against runtime_config_properties.key
- Matches @ConfigurationProperties prefix against runtime_config_properties.key prefix
- Idempotent via deterministic edge IDs
