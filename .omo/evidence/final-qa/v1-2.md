# V1 §2 — FeignClient → Provider endpoint cross-service

Status: FAIL

Expected: `spring_find_feign({name:"X"})` should include target provider endpoint.

Evidence:
- `tool-spring_find_feign.json` and `criteria-session-stdout.json` entry `v1-2-feign-order-service` returned `found:false`, `client:null`, `methods:[]`.
- `post-session-db-counts.json` shows `spring_feign_clients: 0`.
- Source contains `@FeignClient(name = "order-service")` in `examples/springcloud-demo/src/main/java/com/example/order/OrderClient.java:7`.

Conclusion: source artifact exists, but the MCP server did not seed or resolve the Feign client/provider link.
